module UserEventsHelper
  # this helper is giving an undefined method 'redirect_to' error, it works just fine inside user events controller
  # def authorized?
  #   redirect_to root_path, notice: 'Thou Shalt Nought duuu dat :(' unless current_user.id == @user_event.user_id
  # end
end
